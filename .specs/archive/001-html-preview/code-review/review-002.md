**Findings**
- 高: raw URL を `getAttribute('href')` で拾っているため相対 URL のまま流れ、`<base>` も相対 URL になります。[github-dom.ts#L55](/workspace/src/content/github-dom.ts#L55) [github-dom.ts#L74](/workspace/src/content/github-dom.ts#L74) [html-fetcher.ts#L9](/workspace/src/content/html-fetcher.ts#L9) これだと `src/preview.html` 側の sandbox iframe では `chrome-extension://.../owner/repo/raw/...` を基準に解決され、相対 CSS/JS/画像が壊れます。計画にある「absolute href を唯一のソースとする」に反しています。改善案は `link.href` / `rawButton.href` を使うか、`new URL(attr, location.origin).href` で絶対 URL に正規化してから `/blob/ -> /raw/` 変換することです。
- 中: blob ページ用の inline 挿入先が専用実装になっておらず、計画の「ファイルコンテンツ直下」に挿入されません。[github-dom.ts#L106](/workspace/src/content/github-dom.ts#L106) [github-dom.ts#L118](/workspace/src/content/github-dom.ts#L118) [preview-button.ts#L84](/workspace/src/content/preview-button.ts#L84) 現状は Raw ボタンの親要素をそのまま header 扱いし、`[id^="diff-"]` が無ければ `header.parentElement` に落としています。blob ページではツールバー周辺に iframe が入る可能性が高いです。改善案は blob ページ専用に「ファイル本文コンテナ」を取る helper を用意し、PR と blob で container を分けることです。
- 中: preview データ受け渡しを MV3 service worker のメモリ `Map` にだけ載せており、worker 停止で内容が消えます。[background.ts#L1](/workspace/src/background.ts#L1) [background.ts#L8](/workspace/src/background.ts#L8) [preview.ts#L16](/workspace/src/preview.ts#L16) `preview-store` 完了後に worker が休止すると、preview ページの poll は毎回空の `Map` を見るだけになり、タイムアウトへ進みます。改善案は `chrome.storage.session` に一時保存するか、`runtime.connect`/Port など停止に強い受け渡しへ変えることです。
- 低: `.htm` は実装側で HTML 扱いしているのに、blob ページでは content script が注入されません。[public/manifest.json#L17](/workspace/public/manifest.json#L17) [url-utils.ts#L18](/workspace/src/content/url-utils.ts#L18) [url-utils.ts#L55](/workspace/src/content/url-utils.ts#L55) PR Files changed では動く一方、blob の `.htm` 単体閲覧では機能しない不整合があります。改善案は manifest に `.htm` も追加するか、仕様を `.html` のみに揃えることです。

**補足**
前回レビューの修正 4 点はコード上で反映されていました。`pnpm test` は `96 passed`、`pnpm build` も成功しています。
